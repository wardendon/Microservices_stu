server:
  port: 0

# 应用名称
spring:
  application:
    name: cloud-goods-service

  # 配置服务注册中心 nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      username: nacos
      password: nacos